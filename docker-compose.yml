services:
  postgres:
    image: postgres:16
    ports:
      - "55432:5432"
    environment:
      POSTGRES_USER: digest
      POSTGRES_PASSWORD: digest
      POSTGRES_DB: tech_digest
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
