services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: digest
      POSTGRES_PASSWORD: digest
      POSTGRES_DB: tech_digest
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
